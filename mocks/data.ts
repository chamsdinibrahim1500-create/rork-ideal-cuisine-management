import { Project, StockItem, Notification } from '@/types';

export const mockProjects: Project[] = [];

export const mockStockItems: StockItem[] = [];

export const mockNotifications: Notification[] = [];
